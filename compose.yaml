  services:
    postgres:
      image: 'postgres:latest'
      environment:
        - 'POSTGRES_DB=library'
        - 'POSTGRES_PASSWORD=password'
        - 'POSTGRES_USER=user'
      ports:
        - '5431:5432'
