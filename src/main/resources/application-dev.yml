spring:
  liquibase:
    enabled: true

  docker:
    compose:
      lifecycle-management: start-and-stop

logging:
  level:
    org.springframework:
        jdbc: TRACE

management:
  endpoints:
    web:
      exposure:
        include: "*"

